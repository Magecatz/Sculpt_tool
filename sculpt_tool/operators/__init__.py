"""Operator package for Sculpt Tool.

Empty placeholder — no operator classes are registered yet. Later cards
add op_bind.py (OT_bind_garment), op_fit.py (OT_fit_garment),
op_batch.py (OT_batch_fit), and op_pin_groups.py (pin vertex-group
helpers) per ARCHITECTURE.md section 5, along with register()/
unregister() here to wire them into the add-on.
"""
